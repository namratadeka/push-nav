from setuptools import setup

setup(
    name="push_policy",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'numpy',
                      'matplotlib',
                      'torch']
)
